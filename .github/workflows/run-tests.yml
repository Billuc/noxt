name: run-tests
on: [push]

jobs:
  my-job:
    name: my-job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - run: bun install
      - run: rm -rf node_modules/noxt && ln -s $PWD node_modules/noxt
      - run: bun test
