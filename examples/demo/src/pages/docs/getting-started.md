---
title: Getting started
layout: src/layouts/DocsLayout.tsx
---

# Getting started

This page uses YAML frontmatter with a **custom layout**.

- `title` is passed to the layout as a prop
- `layout` resolves to `src/layouts/DocsLayout.tsx`
- The markdown body replaces the layout's children placeholder
