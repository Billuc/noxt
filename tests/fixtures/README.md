# Test project

## Building

`bun build --target=bun --outdir=dist index.ts`

## Running

**Development**: `bun run index.ts`

**Preview**: `cd dist && bun run index.js`

## Cleaning

`rm .cache dist`
